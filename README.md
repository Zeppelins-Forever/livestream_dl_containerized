# livestream_dl_containerized
A docker container and scripts for using livestream_dl conveniently!


Run with:
` sudo docker run -v "$(pwd):/out" livedl3 [URL] ` or ` docker run -v "$(pwd):/out" livedl3 [URL] ` depending on if you have a docker user set up
